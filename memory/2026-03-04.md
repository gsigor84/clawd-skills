# 2026-03-04

- We diagnosed that `/validate` not triggering is likely because text slash-commands are not being parsed by the gateway. Current config shows `commands.native="auto"` and `commands.nativeSkills="auto"` but no `commands.text` flag, which often results in `/...` being treated as plain text.
- Proposed fix: enable `commands.text=true` and ensure skills directory is loaded via `skills.load.extraDirs=["/Users/igorsilva/clawd/skills"]`; offer to patch config and restart gateway.
